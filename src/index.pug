---
title: Designer + Hobbyist Dev
layout: '_base.pug'
pageColorChoice: dark
---

.background-icon(aria-hidden="true")
	svg(version="1.1" class="polygon-icon" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" viewBox="0 0 150 150" style="enable-background:new 0 0 150 150;" xml:space="preserve" aria-hidden="true")
		g 
			polygon(points="118.9,75 150,37.5 112.5,37.5 112.5,0 75,31.1 37.5,0 37.5,37.5 0,37.5 31.1,75 0,112.5 37.5,112.5 37.5,150 75,118.9 112.5,150 112.5,112.5 150,112.5")
section.section.hero-home
	.job-title Designer + Digital Enthusiast
	.hero-text With a love for digital and social design, I strive to keep giving while growing
section.section.work#work 
	h2 My work
	.project-gallery
		- const wantedOrder = ordering.projects
		- const givenOrder = projects 
		- for (let i=0; i < wantedOrder.length; i++) {
		-		let wantedProj = wantedOrder[i].item.data.attributes;
		-		for (let j=0; j < givenOrder.length; j++) {
		-			let givenProj = givenOrder[j];
		-			if (givenProj.title == wantedProj.title) {
		- 			let projCardCover = givenProj.cover.image.data.attributes
		- 			let projCardCoverArea = givenProj.cover.area
						.project-card(class=`col${projCardCoverArea.Start} ${projCardCoverArea.End.toLowerCase()}`)
							a.proj-card-text(href=`projects/${wantedProj.slug}` data-cursor-class="arrow")
								h3.proj-card-title #{wantedProj.title}
							a.proj-card-img-wrapper(href=`projects/${wantedProj.slug}` data-cursor-class="arrow") 
								img.proj-card-img(src=`/_assets/${projCardCover.hash}${projCardCover.ext}` alt="Photo" srcset=`/_assets/${projCardCover.formats.small.hash}${projCardCover.formats.small.ext} 500w, /_assets/${projCardCover.formats.medium.hash}${projCardCover.formats.medium.ext} 1000w, /_assets/${projCardCover.formats.large.hash}${projCardCover.formats.large.ext} 1900w`)
							.proj-card-tag 
								p #{givenProj.tags.data[0].attributes.discipline}
		-			}
		-		}
		-	}
include _includes/_sketchbook.pug
section.section.about
	.about-wrapper
		.about-headers
			h2 About Me 
			//- .about-id-info 
			//- 	//- p.about-name Sami <span class="about-name-last">Moya Medina</span> 
			//- 	//- p.about-pronouns #{about.pronouns}
		.about-text
			.about-synopsis!=`${about.synopsis}`
			a(href="/about" data-cursor-class="arrow").about-linkout  My experience + more
//- include _includes/_finds.pug

